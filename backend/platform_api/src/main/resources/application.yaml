spring:
  application:
    name: platform
  datasource :
    url: jdbc:postgresql://localhost:5432/knowledge_platform
    username: postgres
    password: postgress
  jpa:
   hibernate:
     ddl-auto: update
   show-sql: true
   properties:
      hibernate:
        format_sql: true
server:
  port: 8081